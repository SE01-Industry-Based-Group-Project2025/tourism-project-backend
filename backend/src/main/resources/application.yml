spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sl_tourpal?createDatabaseIfNotExist=true
    username: root
    password: 2468SEnt
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: false

security:
  jwt:
    secret: mySecretKey
    expiration: 3600000

logging:
  level:
    org.springframework: INFO
    com.sl_tourpal.backend: DEBUG
